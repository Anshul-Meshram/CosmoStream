services:
  backend:
    build:
      context: ./backend

    container_name: cosmostream-backend

    ports:
      - "8000:8000"

    volumes:
      - ./backend:/app

    env_file:
      - ./backend/.env.example

    restart: unless-stopped
   
